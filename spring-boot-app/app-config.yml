
# Java context path
server.servlet.context-path: /java

# URL to node app
app.node.url: http://nodejs:8081/

spring:
  jpa:
    properties.hibernate:
      temp.use_jdbc_metadata_defaults: false

  datasource:
    url: jdbc:postgresql://postgresqldb:5432/testdb
    username: postgres
    password: postgres
